#!/bin/bash

for zipfile in *.zip; do
  [ -f "$zipfile" ] || continue
  dirname="${zipfile%.zip}"
  [ -d "$dirname" ] && { echo "Skipping $zipfile (dir exists)"; continue; }
  unzip "$zipfile" -d "$dirname"
done
